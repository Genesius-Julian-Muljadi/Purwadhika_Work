// Q: Write a code that shows 1 if the input is a string, 2 if the input is a number, and 3 for others data type.

const data = false;

if (typeof(data) === 'string') {
    console.log(1);
} else if (typeof(data) === 'number') {
    console.log(2);
} else {
    console.log(3);
};