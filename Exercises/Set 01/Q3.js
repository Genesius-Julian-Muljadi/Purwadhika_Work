// Q: Write a code to find diameter, circumference and area of a circle.

const pi = Math.PI;
const input_radius = 9;

console.log(2 * input_radius);
console.log(2 * input_radius * pi);
console.log(pi * input_radius * input_radius);